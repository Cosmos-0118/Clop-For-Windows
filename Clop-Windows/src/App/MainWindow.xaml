<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="ClopWindows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:ClopWindows.App.Localization"
    Title="Clop for Windows"
        Width="980"
        Height="640"
        MinWidth="820"
        MinHeight="520"
    Background="{DynamicResource Brush.WindowBackground}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource Brush.NavigationBackground}"
                Padding="24"
                BorderBrush="{DynamicResource Brush.NavigationBorder}"
                BorderThickness="0,0,1,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="{loc:Loc app.name}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource Brush.NavigationTitle}"/>
                    <TextBlock Text="{loc:Loc app.tagline}" FontSize="12" Foreground="{DynamicResource Brush.NavigationSubtitle}" Margin="0,4,0,16" TextWrapping="Wrap"/>
                </StackPanel>

                <ListBox ItemsSource="{Binding NavigationItems}"
                         SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource Brush.NavigationTitle}"
                         HorizontalContentAlignment="Stretch"
                         Margin="0,24,0,0"
                         AutomationProperties.HelpText="{loc:Loc navigation.list.help}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" Padding="12" Margin="0,0,0,8" Background="Transparent">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource Brush.NavigationTitle}"/>
                                    <TextBlock Text="{Binding Subtitle}" FontSize="12" Foreground="{DynamicResource Brush.NavigationSubtitle}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Container" Background="Transparent" CornerRadius="8">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Container" Property="Background" Value="{DynamicResource Brush.NavigationItemSelectedBackground}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Container" Property="Background" Value="{DynamicResource Brush.NavigationItemHoverBackground}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Margin="0" Background="{DynamicResource Brush.ContentBackground}">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</Window>
