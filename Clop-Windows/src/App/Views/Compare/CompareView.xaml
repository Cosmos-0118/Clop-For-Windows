<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="ClopWindows.App.Views.Compare.CompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="32,32,32,16">
            <TextBlock Text="Optimise files side by side" FontSize="24" FontWeight="SemiBold" Foreground="#101828"/>
            <TextBlock Text="Choose images, videos, or PDFs to compare and optimise instantly." FontSize="13" Foreground="#475467" Margin="0,8,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="32,0,32,16" Background="#F9FAFB" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="12" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button Content="Browse files" Padding="16,10" Background="#4C6EF5" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Command="{Binding BrowseForFilesCommand}"/>
                <TextBlock Text="or drop files anywhere in this window" FontSize="12" Foreground="#475467" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Margin="32,0,32,32" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="12" Padding="20">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Top" Text="Recent results" FontSize="14" FontWeight="SemiBold" Foreground="#1D2939" Margin="0,0,0,12"/>
                <Grid>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding RecentItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FFFFFF" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding FileName}" FontSize="13" FontWeight="SemiBold" Foreground="#101828" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding SizeSummary}" FontSize="12" Foreground="#475467" Margin="0,4,0,0" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding DurationText}" FontSize="12" Foreground="#475467" VerticalAlignment="Center" Margin="12,0,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="Text" Value="">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                            <Trigger Property="Text" Value="{x:Null}">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>

                    <TextBlock Text="Optimise something to see results here." Foreground="#98A2B3" HorizontalAlignment="Center" VerticalAlignment="Center" FontStyle="Italic">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RecentItems.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
