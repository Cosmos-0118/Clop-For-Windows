<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="ClopWindows.App.Views.Onboarding.OnboardingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32">
            <StackPanel>
                <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Semibold" Foreground="#101828"/>
                <TextBlock Text="{Binding Subtitle}" Margin="0,6,0,0" FontSize="14" Foreground="#475467" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel Margin="0,24,0,0">
                <TextBlock Text="Here's how to get the most from Clop" FontSize="16" FontWeight="Semibold" Foreground="#1D2939"/>
                <ItemsControl ItemsSource="{Binding Steps}" Margin="0,12,0,0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F9FAFB" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="Semibold" Foreground="#101828"/>
                                    <TextBlock Text="{Binding Description}" Margin="0,4,0,0" FontSize="13" Foreground="#475467" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <Border Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" CornerRadius="10" Padding="16" Margin="0,24,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Ready to optimise your first files?" FontSize="15" FontWeight="Semibold" Foreground="#1D2939"/>
                        <TextBlock Text="Drop a folder into the window or let Clop watch your clipboard automatically." FontSize="12" Foreground="#475467" TextWrapping="Wrap" Margin="0,4,0,0"/>
                    </StackPanel>

                    <Button Grid.Column="1" Content="Get started" Padding="14,8" Margin="16,0,0,0"
                            Background="#4C6EF5" Foreground="White" FontWeight="Semibold" BorderThickness="0"
                            Cursor="Hand" Command="{Binding GetStartedCommand}"/>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
