<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="ClopWindows.App.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32">
            <StackPanel>
                <TextBlock Text="Preferences" FontSize="24" FontWeight="SemiBold" Foreground="#101828"/>
                <TextBlock Text="Tweak optimisation behaviours to match the macOS app." FontSize="13" Foreground="#475467" Margin="0,6,0,0"/>
            </StackPanel>

            <Border Margin="0,24,0,0" Padding="20" Background="#FFFFFF" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="Floating HUD" FontSize="16" FontWeight="SemiBold" Foreground="#1D2939"/>
                    <CheckBox Content="Show floating results" Margin="0,12,0,0" IsChecked="{Binding EnableFloatingResults}"/>
                    <CheckBox Content="Hide floating results automatically" Margin="0,8,0,0" IsChecked="{Binding AutoHideFloatingResults}"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Hide after" VerticalAlignment="Center"/>
                        <TextBox Width="60" Margin="8,0,0,0" Text="{Binding AutoHideFloatingResultsAfter, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="seconds" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Margin="0,16,0,0" Padding="20" Background="#FFFFFF" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="Clipboard" FontSize="16" FontWeight="SemiBold" Foreground="#1D2939"/>
                    <CheckBox Content="Optimise clipboard images automatically" Margin="0,12,0,0" IsChecked="{Binding EnableClipboardOptimiser}"/>
                    <CheckBox Content="Copy optimised image back to clipboard" Margin="0,8,0,0" IsChecked="{Binding AutoCopyToClipboard}"/>
                </StackPanel>
            </Border>

            <Border Margin="0,16,0,0" Padding="20" Background="#FFFFFF" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="Automation" FontSize="16" FontWeight="SemiBold" Foreground="#1D2939"/>
                    <CheckBox Content="Watch image folders" Margin="0,12,0,0" IsChecked="{Binding EnableAutomaticImageOptimisations}"/>
                    <CheckBox Content="Watch video folders" Margin="0,8,0,0" IsChecked="{Binding EnableAutomaticVideoOptimisations}"/>
                    <CheckBox Content="Watch PDF folders" Margin="0,8,0,0" IsChecked="{Binding EnableAutomaticPdfOptimisations}"/>
                </StackPanel>
            </Border>

            <Border Margin="0,16,0,0" Padding="20" Background="#FFFFFF" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="Preservation" FontSize="16" FontWeight="SemiBold" Foreground="#1D2939"/>
                    <CheckBox Content="Preserve original timestamps" Margin="0,12,0,0" IsChecked="{Binding PreserveDates}"/>
                    <CheckBox Content="Strip metadata from optimised files" Margin="0,8,0,0" IsChecked="{Binding StripMetadata}"/>
                </StackPanel>
            </Border>

            <Border Margin="0,16,0,0" Padding="20" Background="#FFFFFF" BorderBrush="#EAECF0" BorderThickness="1" CornerRadius="12">
                <StackPanel>
                    <TextBlock Text="Keyboard shortcuts" FontSize="16" FontWeight="SemiBold" Foreground="#1D2939"/>
                    <TextBlock Text="Customise Clop's navigation and global shortcuts." FontSize="13" Foreground="#475467" Margin="0,6,0,0"/>

                    <TextBlock Text="In-app navigation" FontWeight="SemiBold" Foreground="#344054" Margin="0,18,0,0"/>
                    <ItemsControl ItemsSource="{Binding AppShortcutPreferences}" Margin="0,10,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12" Margin="0,0,0,8" Background="#F8F9FC" BorderBrush="#E4E7EC" BorderThickness="1" CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Description}" FontWeight="SemiBold" Foreground="#1D2939"/>
                                            <TextBlock Text="{Binding DefaultBinding, StringFormat=Default: {0}}" Foreground="#667085" FontSize="12" Margin="0,4,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding CurrentBinding}" Foreground="#344054" VerticalAlignment="Center"/>
                                            <Button Content="Change" Command="{Binding CaptureCommand}" Margin="12,0,0,0" Padding="14,4"/>
                                            <Button Content="Clear" Command="{Binding ClearCommand}" Margin="8,0,0,0" Padding="12,4"/>
                                            <Button Content="Reset" Command="{Binding ResetCommand}" Margin="8,0,0,0" Padding="12,4"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="Global shortcuts" FontWeight="SemiBold" Foreground="#344054" Margin="0,18,0,0"/>
                    <ItemsControl ItemsSource="{Binding GlobalShortcutPreferences}" Margin="0,10,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12" Margin="0,0,0,8" Background="#F8F9FC" BorderBrush="#E4E7EC" BorderThickness="1" CornerRadius="8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Description}" FontWeight="SemiBold" Foreground="#1D2939"/>
                                            <TextBlock Text="{Binding DefaultBinding, StringFormat=Default: {0}}" Foreground="#667085" FontSize="12" Margin="0,4,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding CurrentBinding}" Foreground="#344054" VerticalAlignment="Center"/>
                                            <Button Content="Change" Command="{Binding CaptureCommand}" Margin="12,0,0,0" Padding="14,4"/>
                                            <Button Content="Clear" Command="{Binding ClearCommand}" Margin="8,0,0,0" Padding="12,4"/>
                                            <Button Content="Reset" Command="{Binding ResetCommand}" Margin="8,0,0,0" Padding="12,4"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
