<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="ClopWindows.App.Views.Onboarding.OnboardingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:ClopWindows.App.Localization"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="32" MinHeight="520">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0"
                        Background="{DynamicResource Brush.SurfaceBackground}"
                        BorderBrush="{DynamicResource Brush.SurfaceBorder}"
                        BorderThickness="3"
                        CornerRadius="20"
                        Padding="12">
                    <Image Source="/Assets/Brand/Assets.xcassets/clop.imageset/clop_512.png"
                           Width="96"
                           Height="96"
                           Stretch="Uniform"/>
                </Border>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="{Binding Title}" FontSize="28" FontWeight="Semibold" Foreground="{DynamicResource Brush.PrimaryText}" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding Subtitle}" Margin="0,6,0,0" FontSize="14" Foreground="{DynamicResource Brush.SecondaryText}" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1"
                    Background="{DynamicResource Brush.SurfaceSecondaryBackground}"
                    BorderBrush="{DynamicResource Brush.SurfaceBorder}"
                    BorderThickness="3"
                    CornerRadius="20"
                    Padding="24"
                    Margin="0,24,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="{Binding SelectedStep.Title}"
                               FontSize="22"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource Brush.PrimaryText}"
                               TextWrapping="Wrap"/>
                    <TextBlock Grid.Row="1"
                               Text="{Binding SelectedStep.Description}"
                               Margin="0,10,0,0"
                               FontSize="14"
                               Foreground="{DynamicResource Brush.SecondaryText}"
                               TextWrapping="Wrap"/>

                    <ItemsControl Grid.Row="2" Margin="0,18,0,0" ItemsSource="{Binding SelectedStep.Details}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="0,0,0,10">
                                    <Border Width="8" Height="8" CornerRadius="4" Background="{DynamicResource Brush.PrimaryButtonBackground}" Margin="0,6,10,0"/>
                                    <TextBlock Text="{Binding}" Foreground="{DynamicResource Brush.SecondaryText}" FontSize="13" TextWrapping="Wrap"/>
                                </DockPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>

            <Button Grid.Row="2"
                    Content="{loc:Loc onboarding.cta}"
                    Padding="28,12"
                    Margin="0,20,0,0"
                    HorizontalAlignment="Center"
                    Background="{DynamicResource Brush.PrimaryButtonBackground}"
                    Foreground="{DynamicResource Brush.PrimaryButtonForeground}"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Command="{Binding GetStartedCommand}"/>
        </Grid>
    </ScrollViewer>
</UserControl>
