<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="ClopWindows.App.Views.Compare.CompareView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600"
             d:DesignWidth="800">
        <Grid AllowDrop="True"
            DragOver="OnDragOver"
            Drop="OnDropFiles">
        <Canvas IsHitTestVisible="False" Opacity="0.08">
            <Path Stroke="{DynamicResource Brush.SecondaryText}" StrokeThickness="2" Data="M 80 80 L 88 96 L 104 104 L 88 112 L 80 128 L 72 112 L 56 104 L 72 96 Z"/>
            <Path Stroke="{DynamicResource Brush.SecondaryText}" StrokeThickness="2" Data="M 650 110 L 658 126 L 674 134 L 658 142 L 650 158 L 642 142 L 626 134 L 642 126 Z"/>
            <Path Stroke="{DynamicResource Brush.SecondaryText}" StrokeThickness="2" Data="M 560 420 L 566 432 L 578 438 L 566 444 L 560 456 L 554 444 L 542 438 L 554 432 Z"/>
            <Path Stroke="{DynamicResource Brush.SecondaryText}" StrokeThickness="2" Data="M 180 440 L 186 452 L 198 458 L 186 464 L 180 476 L 174 464 L 162 458 L 174 452 Z"/>
            <Path Stroke="{DynamicResource Brush.SecondaryText}" StrokeThickness="2" Data="M 340 180 L 346 192 L 358 198 L 346 204 L 340 216 L 334 204 L 322 198 L 334 192 Z"/>
            <Path Stroke="{DynamicResource Brush.SecondaryText}" StrokeThickness="2" Data="M 720 360 L 726 372 L 738 378 L 726 384 L 720 396 L 714 384 L 702 378 L 714 372 Z"/>
        </Canvas>

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,20,0,20">
            <Button Command="{Binding BrowseForFilesCommand}"
                    Cursor="Hand"
                    Background="{DynamicResource Brush.PrimaryButtonBackground}"
                    Foreground="{DynamicResource Brush.PrimaryButtonForeground}"
                    BorderThickness="3"
                    BorderBrush="{DynamicResource Brush.NavigationBorder}"
                    Padding="36,16"
                    FontSize="22"
                    FontWeight="SemiBold">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                            <Border CornerRadius="20"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                Browse files
            </Button>

            <TextBlock Text="or drop files anywhere in this window"
                       Margin="0,18,0,0"
                       FontSize="14"
                       Foreground="{DynamicResource Brush.SecondaryText}"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
