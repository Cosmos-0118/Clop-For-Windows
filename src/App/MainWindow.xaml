<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="ClopWindows.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:loc="clr-namespace:ClopWindows.App.Localization"
    xmlns:infra="clr-namespace:ClopWindows.App.Infrastructure"
    Title="Clop for Windows"
        Width="1040"
        Height="680"
        MinWidth="860"
        MinHeight="540"
    Padding="{DynamicResource Spacing.WindowPadding}"
    Background="{DynamicResource Brush.WindowBackground}">
    <Window.Resources>
        <infra:AdaptiveSidebarWidthConverter x:Key="AdaptiveSidebarWidthConverter"
                                             MinWidth="220"
                                             MaxWidth="360"
                                             WidthRatio="0.24"/>

        <Style x:Key="AdaptiveSidebarCardStyle" TargetType="Border">
            <Setter Property="Margin" Value="{DynamicResource Spacing.SidebarMargin}"/>
            <Setter Property="Padding" Value="{DynamicResource Spacing.ContentPadding}"/>
            <Setter Property="CornerRadius" Value="{DynamicResource Radius.NavigationCard}"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Width">
                <Setter.Value>
                    <Binding Path="ActualWidth"
                             RelativeSource="{RelativeSource AncestorType=Window}"
                             Converter="{StaticResource AdaptiveSidebarWidthConverter}"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="24"
                                      Opacity="0.18"
                                      ShadowDepth="0"
                                      Color="#FF000000"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Style="{StaticResource AdaptiveSidebarCardStyle}"
                Background="{DynamicResource Brush.NavigationBackground}"
                BorderBrush="{DynamicResource Brush.NavigationBorder}"
                BorderThickness="0">
            <Grid>
                <Border Background="{DynamicResource Brush.NavigationAccentGlow}"
                        CornerRadius="24"
                        Opacity="0.35"
                        Margin="-12"/>
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                        <TextBlock Text="{loc:Loc app.name}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource Brush.NavigationTitle}"/>
                        <TextBlock Text="{loc:Loc app.tagline}" FontSize="13" Foreground="{DynamicResource Brush.NavigationSubtitle}" Margin="0,6,0,24" TextWrapping="Wrap" TextOptions.TextFormattingMode="Ideal"/>
                        <Border Background="{DynamicResource Brush.NavigationBorder}" Height="1" Opacity="0.5" CornerRadius="1" Margin="0,0,0,10"/>
                        <TextBlock Text="{loc:Loc navigation.section.label}" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource Brush.NavigationSubtitle}" Margin="0,8,0,6" TextWrapping="Wrap" TextOptions.TextFormattingMode="Ideal"/>
                    </StackPanel>

                    <ListBox ItemsSource="{Binding NavigationItems}"
                             SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{DynamicResource Brush.NavigationTitle}"
                             HorizontalContentAlignment="Stretch"
                             Margin="0,12,0,0"
                             Padding="6,4"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             AutomationProperties.HelpText="{loc:Loc navigation.list.help}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Margin="2" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="8,6,8,8">
                                    <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource Brush.NavigationTitle}" TextOptions.TextFormattingMode="Ideal"/>
                                    <TextBlock Text="{Binding Subtitle}" FontSize="12.5" Margin="0,6,0,0" TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{DynamicResource Brush.NavigationSubtitle}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                                        <Setter Property="Foreground" Value="{DynamicResource Brush.NavigationTitle}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="2,4"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="SnapsToDevicePixels" Value="True"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Grid Margin="2,0">
                                                <Border x:Name="Indicator" Width="4" CornerRadius="8" HorizontalAlignment="Left" Margin="0" Background="{DynamicResource Brush.NavigationIndicator}" Opacity="0"/>
                                                <Border x:Name="Container"
                                                        Background="Transparent"
                                                        CornerRadius="12"
                                                        Margin="10,0,0,10"
                                                        Padding="14,12"
                                                        BorderThickness="1"
                                                        BorderBrush="{DynamicResource Brush.NavigationItemBorder}"
                                                        SnapsToDevicePixels="True">
                                                    <ContentPresenter/>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Container" Property="Background" Value="{DynamicResource Brush.NavigationItemHoverBackground}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Container" Property="Background" Value="{DynamicResource Brush.NavigationItemSelectedBackground}"/>
                                                    <Setter TargetName="Container" Property="BorderBrush" Value="{DynamicResource Brush.NavigationItemSelectedBorder}"/>
                                                    <Setter TargetName="Indicator" Property="Opacity" Value="1"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource Brush.NavigationTitle}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </DockPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Margin="10,6,0,6" Padding="{DynamicResource Spacing.ContentPadding}" Background="{DynamicResource Brush.ContentBackground}" CornerRadius="{DynamicResource Radius.ContentCard}" BorderBrush="{DynamicResource Brush.SurfaceBorder}" BorderThickness="1">
            <ContentControl Content="{Binding CurrentView}"/>
        </Border>
    </Grid>
</Window>
